
## 追加：embedding/量化运行/日志
- 本地 embedding 修复：安装 Node24 + node-llama-cpp 后，`openclaw memory status --deep` 显示 provider=local OK；memory_search 需在 Node24 进程下才能正常。gateway restart 曾因 OOM 失败。
- 轮跑下单：新增 `scripts/pm_auto_runner_multi.sh`（合并轮跑 br_tail_v1 + test1/test2/test3），日志写入 `data/polymarket/runtime/pm_auto_runner_multi.log`，含 RUN/OK/ERROR/STOP 与 orders/results/reasons。
- 下单软件数据现状：orders=0，主要过滤原因 `too_far_end/no_end/no_pick`；原因是短周期市场池不足，扫描文件 `market_scan_2026-03-16.jsonl` 样本太小。
- Polymarket 页面验证：官网存在 5m “Up or Down” 市场；`market_scanner.py` 仅筛 `p_yes<0.3 & vol>50000` 导致短周期被过滤，需改扫描逻辑。
- openclaw browser 脚本：`scripts/openclaw_browser.js`（基础）与 `scripts/openclaw_browser_expand.js`（进阶）。
