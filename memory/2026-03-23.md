# 2026-03-23

- Jim强调：策略与执行不要反复询问；默认直接执行，只有权限/风险边界才问。
- 关键运行脚本：`scripts/pm_5m_last60_runner.sh`（已改为尾盘扫描，当前为“最后20s开始扫描”，1s轮询），日志输出 `/tmp/pm_5m_runner.log`，尾盘数据写 `data/polymarket/runtime/last20_window.jsonl`。
- 强制仅匹配5m市场：`pm_paper_loop.py`在 `use5mSlug` 时只保留 slug 含 “-5m-”。
- 去重规则修复：dedup 改为“策略+市场”维度，不再因同市场阻断其它策略。
- 日志增强：`too_far_end` 记录 `secsToEnd/maxSecsToEnd/endTs/nowTs`，并附价格区间 `maxPrice/minPrice`。
- 策略多次重建：最新一轮为 5s/10s 窗口 + 价格 0.75/0.8/0.85/0.9（8组），仅测 5m BTC/ETH，旧策略归档到 `strategies/_archive/`。
- Jim明确：扫描从倒数20s开始；下单只在10s/5s；尾盘20s价格不稳定，接近关闭才稳定。
- 数据新鲜度/可预测性排查清单已制定；A: last20时间戳与订单最新时间戳；B: 尾盘价格区间 vs 阈值；C: runner单实例、5m slug、match_debug ；D: orders/results更新。
- SOUL.md已新增“Understanding Accuracy & Rigor”与“Reasoning & Understanding Protocol”硬规则，并提交（commit d8a2e16）。
- Jim要求大脑规则层问题分析为主，不落盘业务细节；后续需按规则优先级、三锚点、单链路执行、证据先于结论执行。
