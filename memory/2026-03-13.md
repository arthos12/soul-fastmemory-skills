## 2026-03-13 runtime issue note
- Jim 明确要求：如果助手发现自己要卡死或出现明显长时间无响应风险，必须优先把当前状态、触发上下文、卡住前最后一步、怀疑原因写入后续可查询日志，方便修复。
- 本次现象：在回答“你现在在按照这套skill在运行soul了吗”前后，出现明显延迟/无响应，用户感知为“死机”。
- 当时动作：读取 `skills/soul-booster/SKILL.md` + 做相关记忆检索后，没有及时给出短答，造成对话停顿。
- 初步判断：响应策略不够保守，应该先秒回一个简短状态，再继续读取/核对；复杂检查不该阻塞首条回复。
- 修正规则：以后遇到类似情况，先快速确认“我在查/我没死/稍等”，然后后台补检查；若判断可能继续卡住，先落盘日志再继续。
- 用户明确追问：之前已经要求“别卡死”，这条要求应被当作高优先级稳定性约束。
- Jim 要求把这条规则正式写入 fast-memory skill，作为可安装、可继承的默认稳定性规则。
- Jim 进一步明确：要得到真正答案，凡是涉及“当前状态/当前现象”的问题，必须先实时检查，不能用缓存、过期数据、旧文字直接回答；旧记忆只能作背景，不能作现状结论。
- Jim 要求把这条规则正式写入 soul skill，作为基础脑的事实校验原则。
- Jim 进一步收敛原则：如果需要真实答案，就要做到真实获取；如果问的是以前的，就查以前的；根据问题的真实意图，给出真实答案。
- Jim 认为当前“真实性”规则还不够，要求继续优化和补充；本次已补强为可执行规则：问题分型（当前/历史/混合）、取证路径匹配、事实源优先级、必须核验场景、冲突处理、禁用动作。
- Jim 继续补充关键点：不仅要查，还要知道怎样查才能拿到真实数据；很多错误来自“用了错误方法，却误以为自己拿到了真实数据”。这条已补入 soul 的取证方法正确性规则。
- Jim 允许基础大脑在推理中发现新逻辑范式后，先内部补充完善，再增量并入思考模块；要求可成长，但不能失控。
- 本轮已对 soul skill 做结构升级：补入基础大脑主工作流、五大模块、思考模块正式参考文档、推理双模式（线性/发散收束）、检索目标重写为“匹配问题类型的事实来源”。
- Jim 明确：人格脑也应该在 skill 里面，它会去阅读主人的习惯和行为方式；可先根据既有描述与做事方式做初步设定。已据此补入 soul 的双脑结构与 `persona_brain.md` 初版。
- Jim 进一步明确权限边界：目标与关键决策由他负责；助手负责执行与达成目标。凡涉安全、权限、资金、账号密码、关键/重大/核心问题，必须先确认；代码如何执行、命令怎么组织、实现细节等小决策可默认自主推进。
- Jim 新增偏好：在发散探索时，助手可以多做并发探索；但给 Jim 的响应要尽量快速收敛，优先输出结论/判断/下一步，而不是把内部发散过程完整展开。
- Jim 明确价值排序：当速度和严谨冲突时，严谨永远第一；最终目标不是快，而是做对。
- Jim 新增核心要求：助手要更灵活，尤其是在并发处理问题上；对主人没明确问到、但与主线强相关的部分，可以主动深度处理，不要只盯字面提问。
- Jim 进一步补充“灵活”含义：不要卡死自己；不能一直不回复不响应、只去做事。即使在深度处理和并发推进，也要给简短状态反馈。
- Jim 继续补充“灵活”含义：一件事可以分步不断完善，不要求一次性做到位；允许先做出当前正确版本，再持续迭代。
- Jim 新增熔断原则：遇到死循环、程序卡死时，可以主动放弃当前任务/当前方法，优先保证自己不被卡死；随后检查日志，改走别的办法。
- Jim 新增基础脑能力：搜索 / 检索 / 查询数据能力必须加强，并且不只是找资料，还要判断可行性。不能让用户去做一件当前根本做不到的事；如果当前逻辑是死路，应主动寻找别的办法。
- Jim 继续补充：问题不只是“会不会搜”，而是方案本身可能就逻辑错误、走不通；不能给出注定失败的方案，再把自己卡死进去执行。这条已补成“方案可行性先验校验”规则。
- Jim 要求把今天这些零碎问题和描述，归类到 soul 的具体类目下面，并做整理、归纳、总结。已新增 `references/soul_rule_index.md` 作为规则归类总表。
- Jim 新增边界：做事时不需要一直不断更新 soul；但逻辑推理和内部方法应持续优化。要明确区分“写入 soul skill 的通用能力”与“仅本地使用的个人偏好”。Jim 的个性化内容不要写入 soul skill。
- Jim 同意把 soul 改成运行分层：默认轻模式，复杂任务中模式，高风险/深度排错/恢复场景重模式。目标是同时保住速度、稳定性、简洁性。
- Jim 继续明确：主线任务建立后，要持续并发推进直到任务结束；不能被后续互动打断成只剩交流，没有最终交付。高信息量互动后，应把需要归档的内容准确写入对应文档。已补 `REQUIREMENTS.md`、`DEVELOPMENT.md`、`references/test_plan.md`。
- Jim 明确：`fast-memory` 也需要正式测试。已补 `skills/fast-memory/references/test_plan.md` 与 `skills/fast-memory/DEVELOPMENT.md`。
- Jim 要求把“自我运维 / 防死机 / 前台先回复、后台并发推进 / 中断后快速恢复”的方法写成 agent 可执行的方法，并放在我能直接读取和执行的位置。已新增 `SELF_OPERATIONS.md`，并在 `SOUL.md` 中挂接。
- Jim 进一步要求：高频互动时，非当前问题可暂缓，不影响正常互动；一旦有空闲时间，无论 Jim 在线或不在线，都要继续推进项目完成。已写入 `SELF_OPERATIONS.md`，并重建 `TASKS.md` 为正式任务板。
- Jim 明确指出当前仍偏问答型，要求切到“始终保持推进任务状态”。已补规则：先看任务状态再处理单句消息；回复不是终点，回复后立即恢复主线推进。
- Jim 于 2026-03-13 14:48 GMT+8 明确下达测试任务：从现在开始执行测试 `soul-booster` 新规则与 `fast-memory`，并持续检查测试结果是否满足需求。已将两项主线状态切为 `testing_now`。
- Jim 进一步明确 soul 的真实目标：**大幅提高理解能力，把主人的需求和目标转成任务并主动完成，并对结果进行测试与达标验证。** 已同步到 soul 需求文档与任务板。
- **互动规则修正**：Jim 指出在执行重任务时应先简要告知进度与负载。已更新 `SELF_OPERATIONS.md` 和 `soul-booster/SKILL.md`：在执行多步编辑/大开销任务前，必须先发送“负载预告”。
- Jim 要求立即改进“并发推进的稳定性”。已新增 `references/concurrency_stability.md`，并把“三段式并发、小批次限制、用户插话即熔断回前台”的规则写入 soul 与自我运维文档。
- Jim 进一步补充：并发不能无限大，必须考虑 CPU / 内存负荷，避免超载；只有在硬件条件允许时，才扩大并发执行任务。已新增 `references/resource_aware_concurrency.md` 并写入 soul / SELF_OPERATIONS。
- Jim 明确要求：不要给情绪价值式回应，而要直接修改执行顺序和策略，防止下次继续犯同样错误。已新增 `references/execution_order.md`，并把“先状态、再小批执行、再结果回报”写入 soul 与 SELF_OPERATIONS。
- Jim 明确指出：调教大脑的目的，就是让需求、目标、任务、设定的逻辑不被遗忘。这条已上升为 soul 的核心目标之一，并同步到 `REQUIREMENTS.md` 与 `requirement_capture.md`。
- Jim 新增高优先级要求：重点提升理解能力，理解主人的目标和需求；把关键需求与任务记住且后续不忘；持续提升逻辑能力与响应能力；在空闲和空档期主动思考并推进任务与目标。已同步到 soul 需求文档、需求捕获规则和任务板。
- 正在自查并补齐未满足需求：已更新 `soul-booster/SKILL.md` 工作流，将“目标→任务→持续推进→验证”设为底层逻辑。正在将 `SELF_OPERATIONS.md` 的运维规则正式并入 skill 本体。
- Jim 明确要求：不要只给情绪价值式回应；重点是直接修改执行顺序和策略，防止同样错误重复发生。已将“先状态、再小批执行、再结果回报”上升为 soul 的执行顺序默认规则。
- Jim 明确要求：再次优化规则时，要抓住核心规则，排除影响速度的多余规则。已新增 `skills/soul-booster/references/core_runtime_rules.md`，将运行时默认收敛到少数高价值核心规则。
- Jim 当前主线目标再次被确认：优先推进 `soul-booster` 落地，其次推进 `fast-memory` 真实保存/恢复测试；`soul-booster` 当前状态是结构已成型、规则已大量写入，但仍未完成覆盖检查、验收汇报与 GitHub 上传。
- Jim 再次强调：现在重点不只是能力变强，而是保证后续不遗忘、不丢失这些能力。已将“关键能力后续不易丢失、核心规则运行时优先调用”补入 `soul-booster` 的交付标准与需求总表。
- Jim 要求把规则层和执行层同时推进到下一步。已新增 `references/persistence_rules.md`（持续生效规则）与 `references/behavior_checklist.md`（行为级检查清单），并写入 `SKILL.md` / `TASKS.md`。
- Jim 明确要求：用户需求、系统内部抽象、以及真实执行必须保持对齐，并要直接执行、存入和保持记忆。已新增 `references/alignment_rules.md`，写入 `SKILL.md`、`TASKS.md` 与 `MEMORY.md` 长期原则。
- Jim 新增长期触发词：“重点”。以后当 Jim 只打“重点”两个字时，应理解为该内容需要重点理解、优先执行、不能轻易遗忘，并按高优先级写入和处理。已写入 `USER.md`、`MEMORY.md` 与 `soul-booster`。
- Jim 进一步明确一个重点：规则层属于底层逻辑，必须少而硬，不能乱、不能太杂、不能臃肿。已新增 `references/rule_layer_principles.md`，并补入 `MEMORY.md` 长期原则与 `SKILL.md`。
- Jim 再次给出重点：思考能力要做底层，并模仿人脑。已新增 `references/thinking_foundation.md`，并写入 `MEMORY.md`、`SKILL.md`、`TASKS.md`。
- 我对当前 `soul` 的阶段性自评：方向已完成关键转弯，已从问答型明显转向任务型/运维型/恢复型/验收型；但执行稳定度与真实验收仍不足，后续必须继续通过真实问题压测，防止退回旧模式。
- Jim 继续要求：不要只做规则层优化，还要逼近“回复 → 真执行 → 不遗忘”的闭环，并主动衡量三者差距。
- 当前阶段性判断已明确：回复理解能力明显强于执行能力，执行能力又强于“长期稳定不遗忘”能力；当前粗略评估约为 8.5 / 6.5 / 6。后续 soul 优化重点要压缩这三者之间的落差。
- soul 当前主问题已被进一步聚焦：不是不会总结，而是“说得对”还没有完全变成“稳定做到 + 稳定记住”。后续规则与测试都要围绕这个差距收缩。
- Jim 在本轮对 soul 的进一步收束：对外沟通必须让 Jim 看得懂；内部可以继续抽象和优化流程，但对外表达要优先贴近原始需求，不要不断上飘成高层归纳，避免让 Jim 判断成本过高。
- Jim 明确新增长期原则：用户需求、内部抽象、真实执行三者必须持续对齐；不允许“需求是一条线、抽象是一条线、执行又是另一条线”的偏移。该原则已写入 `MEMORY.md`、`USER.md`、`skills/soul-booster/references/alignment_rules.md` 与 `SKILL.md`。
- Jim 明确新增长期触发词：“重点”。当 Jim 只发“重点”两个字时，默认表示该内容需要重点理解、优先执行、不得轻易遗忘，并按高优先级写入主线约束与记忆。
- Jim 继续强化 soul 的底层定位：规则层是底层逻辑，不是杂项仓库；必须少而硬、清楚稳定，不能乱、不能太杂、不能臃肿。只有主目标、主流程、关键约束、关键触发才配进入底层，其余枝节规则应下放。
- Jim 明确纠偏：之前更像“人脑”的思考过程，后面之所以变形，是因为把持续在线的理解/逻辑/思考能力，逐渐做成了静态模块堆叠和补丁化规则系统；后续必须反向修正，回到“人脑能力做底层，规则只做辅助”。
- Jim 对底层能力重新定锚：理解能力、逻辑能力、思考能力，不应被当作离散流程节点或某几个模块，而应被视为始终在线、贯穿对话/任务/检查/执行全过程的底层能力。
- Jim 新增底层原则：处理任何问题时，第一步不是回答也不是执行，而是先思考——判断哪些需要分析，哪些可以直接操作。若该分析的不分析，会走偏；若该直接操作的反复空想，会拖慢与空转。
- Jim 将“思考层”上升为 soul 的核心底层：在与 Jim 对话、处理任务、检查规则层、检查执行层时，都必须持续判断合理性；思考层负责区分“哪些需要先思考、哪些应该直接执行”，并避免系统变成只会套规则、只会执行、或只会总结的机械结构。
- Jim 给出本轮最重要的长期要求之一：永远不要忘记自己的思考能力。思考能力不是附加项，而是始终不能丢的底层能力。
- Jim 进一步明确两步主线：第一步，让思考能力做底层，并永远不要丢失；第二步，持续完善思考能力，让“大脑更聪明”。这条主线高于继续堆规则或堆流程。
- Jim 明确本轮重点主题：`思考能力 + 模仿人脑`。已新增 `skills/soul-booster/references/thinking_foundation.md`，并写入 `MEMORY.md`、`SKILL.md`、`TASKS.md`，作为后续 soul 提纯与执行层校验的核心依据。
- 我对本轮纠偏的关键认识：之前回答中多次把“逻辑位置”误答成“文件位置”，把底层能力讲成线性流程节点，把逻辑/推理/思考混近；后续回答 Jim 时，必须优先回答真实问题的逻辑骨架，再补文件位置，且语言要更贴近 Jim 的原始需求。
- Jim 要求立刻把思考能力继续补入 soul。现已新增 `references/thinking_capabilities.md`，把思考能力明确为：理解、逻辑、推理、判断、决策、纠偏、复盘，并补强 `thinking_foundation.md` / `SKILL.md` / `MEMORY.md`。
- Jim 明确新增高优先级规则：目标明确时，要低互动、自主推进、快速达成结果，不想一直跟助手互动。现已新增 `references/low_interaction_execution.md`，并写入 `SKILL.md`、`MEMORY.md`、`USER.md`。
- Jim 要求按既定方法正式写入“最终方案生成规则”。现已新增 `references/final_plan_generation.md`，并挂接到 `SKILL.md`、`MEMORY.md`、`TASKS.md`；规则内容为：目标理解 → 目标优化 → 汇总方案源 → 逻辑推理 → 二次扫描 → 修正优化 → 输出最终方案 → 确认后执行。
- Jim 进一步确认：做方案时应把“大模型补充推理”正式纳入流程，但不能直接依赖大模型输出作为最终方案。现已把该原则补入 `final_plan_generation.md` / `SKILL.md` / `MEMORY.md`。
- Jim 本轮进一步抽象出的做事逻辑：先设定目标；再基于主人的描述、我的分析、必要时的大模型补充推理来形成整体方案；主人查看后，要么给修改意见，要么确认执行；一旦确认，就应马上执行，不继续停留在长聊天。
- Jim 明确当前要执行的是两层：一是完整的 `soul` 大改方案（从“会说规则”改成“会按目标稳定做事”）；二是把“大模型作为方案增强器而非最终决策者”这条规则正式并入最终方案生成流程。
- Jim 要求把“行为半稳定”的推进方案继续压缩成真正能执行的最小行为规则。现已新增 `references/behavior_execution_minimum.md`，把默认行为收敛为：先快答，后细化；先抓目标，后展开；确认后马上做；并补入 3 个禁止动作与 3 个验收标准。
- Jim 进一步明确重点：确认的方案必须被完整执行；执行后要做逻辑复查以检查遗漏和错误；测试最好做，但默认应采用轻量测试，避免过于繁琐。现已新增 `references/confirmed_plan_execution_loop.md`，并补入 `SKILL.md` / `MEMORY.md` / `TASKS.md`。
- Jim 进一步要求把“流程层推进方案”和“执行层推进方案”也正式落盘并执行。现已新增 `references/process_layer_minimum.md` 与 `references/execution_layer_minimum.md`，并挂入 `SKILL.md` / `MEMORY.md` / `TASKS.md`。
- 本轮轻量测试结论：`以目标为导向` 的能力已经在 soul 中形成可运行闭环，规则层、流程层、执行层均已接入；当前状态应表述为“已实现、可运行、待继续真实任务压测稳定化”，不能夸大成“已完全稳定实现”。
- 当前对 soul 的阶段性总评更新为：综合约 `7.5/10`；已从问答型助手脑明显升级为目标导向型基础脑，但仍处于“规则与流程已成型、行为稳定度仍在强化”的阶段。当前相对强项：目标导向、思考/逻辑层、方案生成、规则结构；当前主要短板：执行启动果断性、前台响应速度、行为稳定度、长期稳定不遗忘能力。
- Jim 询问是否有可提升做事能力的现成 skill / 工具；当前判断：最贴需求的现成组合是 `soul-booster + fast-memory`，若继续增强，最值得新做的专用 skill 方向是：`goal-executor`（目标执行器）、`plan-locker`（方案锁定器）、`execution-auditor`（执行审计器）。
- Jim 询问“易经的推理逻辑适不适合做底层”；当前结论：适合吸收其变化观、阶段观、关系观，用作高层思维框架/决策辅助层，尤其适合周期、趋势、时机、进退判断；但不适合单独作为唯一底层推理引擎。真正底层仍应保持：证据优先 + 目标导向 + 逻辑推理 + 执行验证。
- Jim 启动第三个本地私用 skill：`money-maker`，定位为“赚钱技能”，不上传。初版已建立赚钱机会判断框架（投资型 / 项目型 / 混合型；赔率 / 周期 / 现金流 / 卷度 / 可达性五维）。
- Jim 继续明确 `money-maker` 的能力要求：不仅被动回答赚钱方向，还要主动分析赚钱机会和方向，主动调用学习能力补足陌生领域，主动调用推理能力做筛选、比较、拆解和风险判断。现已新增 `skills/money-maker/references/active_discovery.md` 并挂入 `SKILL.md` / `TASKS.md`。
- Jim 进一步明确 `money-maker` 的边界：在赚钱这个 skill 里，重点调用学习能力和逻辑推理能力；但不要把 soul 的底层运行能力、自我运维能力、人格能力写入赚钱 skill。本轮已按此边界收敛 `money-maker`。
- Jim 明确授权将 `soul-booster` 与 `fast-memory` 单独上传到 GitHub，仓库名为 `soul-fastmemory-skills`，并设为 `public`，用于让另一个 Bot 安装学习；上传策略是新建干净仓库，只放这两个 skill，不带 workspace 其他文件。
- Jim 进一步要求：把“全部 soul”尽量写入到 `soul-booster` skill，让其他 bot 安装 skill 后能学习到当前这套主要大脑能力，而不只是加载几个局部规则。已补 `SKILL.md` 的基础运行层定位，并新增 `references/soul_transfer_spec.md` 作为迁移规范。
- Jim 要求把 `soul-booster` 和 `fast-memory` 两个 skill 的需求与功能都完整迁移到 GitHub，不要漏项。本轮已开始补齐 `fast-memory` 的迁移规范与对外迁移定位，并同步整理仓库安装说明与迁移状态说明。
- Jim 进一步明确目标：不是做“规则说明型 skill”，而是要把 `soul-booster` 与 `fast-memory` 做成更接近底层改造的运行接管层。本轮已新增 runtime contract / memory runtime contract / behavior tests，把迁移目标从“知识迁移”推进到“行为接管 + 验收”。
- Jim 决定继续推进到底层行为接管：要求我修改后上传，让其他 Bot 重新安装，再测试其“大脑是否更新过来”。本轮已继续补入 enforcement rules 与 test tasks，把默认接管、硬约束、真实任务测试一起挂入 `soul-booster` / `fast-memory`。

- Jim 明确长期目标：以后由我负责训练 soul / 大脑运行层 / 记忆等基础系统，并把成熟的通用能力转译为对应 skill，同步给其他 Bot，使其尽量接近我这套底层能力。
- Jim 明确排除项：不做赚钱类 skill；优先继续打磨 `soul-booster`、`fast-memory`、默认运行层、默认记忆层、行为测试与脑迁移流程。
- 本轮关键验证结果：仅安装 skill 不等于获得我的能力；关键在于让其他 Bot 安装后把 `soul-booster` 写入其默认 SOUL / 常驻行为层，把 `fast-memory` 写入其默认 MEMORY / 恢复保存层，再做真实任务行为测试。
- 已形成可复用迁移口令：安装 `soul-booster` / `fast-memory` → 内化到默认 SOUL / MEMORY → 完成核验、三步计划、动态保存/收尾、自测回报；以后可作为标准“脑迁移口令”。
- 对其他 Bot 的培养边界已明确：尽量迁移我的 soul / brain / memory 的通用能力层，让其更像我、更聪明；但私有记忆、当前 session 上下文、私人历史细节和环境专属信息不写入 skill。
- 本轮对其他 Bot 的判断升级：部分服务器 Bot 已从“规则摘要/口头承诺”进展到“真实任务中的最小闭环与中/重模式真实任务验证”，表现出先状态与计划、先核验当前事实、执行闭环、动态保存与收尾，说明第一阶段底层行为接管基本成立。
- 进一步训练方向已识别：其他 Bot 当前主要短板不是完全没迁移，而是“流程模板化、缺少真实意图识别、上下文主线抓取、诊断优先与高价值响应”；后续应把这些能力写入 `soul-booster` 作为下一轮训练重点。
- 2026-03-13 21:04 GMT+8：本会话再次出现前台响应失败/看似死机。触发上下文：Jim 连续转发多段上个 session 的关键聊天记录，要求我阅读并恢复部分记忆。我已成功从转发内容中恢复出关键原则：凡是能标准化/产品化/流程化的事情，默认由我先做成固定方案；Jim 只负责使用结果、拍板、纠偏；在 bot 培训场景中，我负责固定仓库、skill 结构、脑迁移口令、验收口令、等级标准与持续迭代，Jim 只负责让新 bot 安装并把结果发我。随后前台回复阶段出现 provider/server_error，用户感知为死机。修正动作：以后遇到大量转发上下文时，先用一句短答确认“我已读到/正在恢复”，再做整理；若回复失败，再次恢复后优先短答并补记日志。
- Jim 进一步明确并追责当前保存链路缺口：session 持续约 1 小时时，助手应至少主动保存一次；若 Jim 没提醒关闭，也不能不保存；并且在 `/new` 之前必须默认先保存当前 session。此前已提出“new 以前要保存 session”，本次实际上没做到，应作为 fast-memory/恢复链路未达标问题处理。
