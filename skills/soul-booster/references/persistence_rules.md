# Persistence Rules

## 目标
让需求、方案与能力不只存在于当前回答里，而能持续生效。

## 规则
1. 新识别出的关键需求，必须先判断是否属于目标 / 任务 / 约束 / 验收标准。
2. 关键需求一旦识别，必须先写入对应文档，再继续执行。
3. 新消息到来时，默认先合并旧的关键约束，不允许直接把旧要求冲掉。
4. 新练出来的关键能力，必须进入运行时优先调用的核心规则，而不是只停留在总结里。
5. 后续对话、新 session、长上下文中，都要优先恢复这些核心规则与主线目标。

## 验收点
- 不是只在本轮回答表现好
- 而是在后续运行中也不轻易丢失
