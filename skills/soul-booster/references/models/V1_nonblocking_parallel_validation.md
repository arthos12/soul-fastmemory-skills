# V1 非阻塞并发验证（Non-blocking Parallel Validation）

## 一句话定义
验证不应该阻塞推进：验证可以并发跑，且验证启动后就继续做下一件事；用“快照/汇总”异步查看结果，不要为了等窗口而停工。

## 规则
- **不设固定等待窗口**：不因为“要看30分钟数据”而停工。
- **验证并发**：互不依赖的验证同时跑；只对“底座依赖”做必要串行。
- **验证结果异步汇总**：用快照脚本随时读取最近 N 次/最近 T 分钟的统计；不要求等待满 T。

## 验收
- 用户问效率时：立即给当前快照；同时继续推进任务，不暂停。
