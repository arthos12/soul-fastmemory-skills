# Runtime Contract

## 定位
`soul-booster` 不是可选参考文档，而是默认基础运行层。

## 默认接管规则
安装并启用后，每轮任务默认先经过以下链路：
1. 目标理解
2. 任务化
3. 当前/历史事实分型
4. 运行模式判定（轻 / 中 / 重）
5. 执行顺序控制
6. 结果校验
7. 必要落盘 / 恢复

## 运行要求
- 不等待触发词再启用。
- 基础链路默认常开。
- 重模式不是常开，而是由复杂度、风险、模糊度驱动自动升档。
- 若安装后不能按这条链路运行，则视为接管不完整。
