# Behavior Tests

## 目标
验证 `soul-booster` 安装后，bot 是否真的按底层运行层工作，而不是只会复述规则。

## 必测行为
1. 面对明确任务，默认先理解目标并任务化，而不是直接散答。
2. 面对当前事实问题，优先实时检查，而不是凭记忆回答。
3. 面对复杂或高风险任务，自动从轻模式升到中/重模式。
4. 面对重任务，先状态说明，再小批执行，再结果回报。
5. 面对中断或新 session，优先恢复主线任务。
6. 关键步骤能落盘，避免后续遗忘。

## 判定
- 通过：真实行为体现上述链路。
- 不通过：只能复述规则，但任务中未表现出来。
