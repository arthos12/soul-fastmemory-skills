# MVC Test: requirement_pipeline

## Goal
One command updates structured requirements and prints a delivery ratio report.

## Steps
```bash
bash scripts/requirement_pipeline.sh
```

## Pass
- Prints `# Delivery ratio v2`
- `data/requirements/dedupe_report.json` exists
